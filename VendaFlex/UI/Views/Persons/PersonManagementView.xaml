<Page x:Class="VendaFlex.UI.Views.Persons.PersonManagementView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:converters="clr-namespace:VendaFlex.Infrastructure.Converters"
      xmlns:entities="clr-namespace:VendaFlex.Data.Entities"
      mc:Ignorable="d" 
      d:DesignHeight="900" 
      d:DesignWidth="1440"
      Background="{DynamicResource MaterialDesignPaper}">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
        <converters:BooleanToStatusConverter x:Key="BooleanToStatusConverter"/>
    </Page.Resources>

    <Grid>
        <!-- Loading Overlay -->
        <Grid Background="#AA000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Panel.ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True" Width="60" Height="60"/>
                <TextBlock Text="Carregando pessoas..." Foreground="White" Margin="0,16,0,0"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
            </StackPanel>
        </Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="24,16"
                                      materialDesign:ElevationAssist.Elevation="Dp4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountMultiple" Width="36" Height="36" 
                                                 VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Gestão de Pessoas" 
                                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                            <TextBlock Text="Gerencie clientes, fornecedores e funcionários" 
                                       Style="{StaticResource MaterialDesignCaptionTextBlock}" Opacity="0.8"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Quick Stats -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:Chip Margin="0,0,8,0" IconBackground="#4CAF50">
                            <materialDesign:Chip.Icon>
                                <TextBlock Text="{Binding TotalCustomers}" FontWeight="Bold" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </materialDesign:Chip.Icon>
                            <TextBlock Text="Clientes"/>
                        </materialDesign:Chip>
                        <materialDesign:Chip Margin="0,0,8,0" IconBackground="#FF9800">
                            <materialDesign:Chip.Icon>
                                <TextBlock Text="{Binding TotalSuppliers}" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </materialDesign:Chip.Icon>
                            <TextBlock Text="Fornecedores"/>
                        </materialDesign:Chip>
                        <materialDesign:Chip Margin="0,0,16,0" IconBackground="#9C27B0">
                            <materialDesign:Chip.Icon>
                                <TextBlock Text="{Binding TotalEmployees}" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </materialDesign:Chip.Icon>
                            <TextBlock Text="Funcionários"/>
                        </materialDesign:Chip>

                        <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                Command="{Binding LoadDataCommand}" ToolTip="Atualizar" Margin="0,0,8,0">
                            <materialDesign:PackIcon Kind="Refresh"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                Command="{Binding AddCommand}" ToolTip="Adicionar Pessoa">
                            <materialDesign:PackIcon Kind="Plus"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:ColorZone>

            <!-- Filters Panel -->
            <materialDesign:Card Grid.Row="1" Margin="16,16,16,0" Padding="16"
                                 materialDesign:ElevationAssist.Elevation="Dp2">
                <Expander Header="Filtros de Busca" IsExpanded="True"
                          materialDesign:ExpanderAssist.HorizontalHeaderPadding="0">
                    <Expander.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FilterVariant" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding}" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                            </StackPanel>
                        </DataTemplate>
                    </Expander.HeaderTemplate>
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Row 1: Text Filters -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     materialDesign:HintAssist.Hint="Buscar por nome, email, telefone ou documento..."
                                     materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                     materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                                     materialDesign:TextFieldAssist.HasClearButton="True">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>

                            <ComboBox Grid.Column="2" SelectedValue="{Binding SelectedTypeFilter, Mode=TwoWay}"
                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                      materialDesign:HintAssist.Hint="Tipo de Pessoa"
                                      materialDesign:TextFieldAssist.HasClearButton="True">
                                <ComboBoxItem Content="Todos" Tag="{x:Null}"/>
                                <ComboBoxItem Content="Clientes" Tag="{x:Static entities:PersonType.Customer}"/>
                                <ComboBoxItem Content="Fornecedores" Tag="{x:Static entities:PersonType.Supplier}"/>
                                <ComboBoxItem Content="Funcionários" Tag="{x:Static entities:PersonType.Employee}"/>
                                <ComboBoxItem Content="Cliente e Fornecedor" Tag="{x:Static entities:PersonType.Both}"/>
                            </ComboBox>

                            <ComboBox Grid.Column="4"
                                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                      materialDesign:HintAssist.Hint="Status"
                                      materialDesign:TextFieldAssist.HasClearButton="True">
                                <ComboBoxItem Content="Todos"/>
                                <ComboBoxItem Content="Ativos"/>
                                <ComboBoxItem Content="Inativos"/>
                            </ComboBox>
                        </Grid>

                        <!-- Row 2: Actions -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding ClearFilterCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Margin="0,0,12,0">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FilterRemove" Margin="0,0,8,0"/>
                                    <TextBlock Text="Limpar Filtros"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding SearchCommand}"
                                    Style="{StaticResource MaterialDesignRaisedButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Magnify" Margin="0,0,8,0"/>
                                    <TextBlock Text="Buscar"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Expander>
            </materialDesign:Card>

            <!-- Main Content: Persons List -->
            <materialDesign:Card Grid.Row="2" Margin="16" Padding="0"
                                 materialDesign:ElevationAssist.Elevation="Dp2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- List Header -->
                    <Border Grid.Row="0" Background="{DynamicResource MaterialDesignToolBarBackground}" Padding="16,12">
                        <Grid>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FormatListBulleted" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Pessoas Cadastradas" Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                                <materialDesign:Chip Margin="12,0,0,0" Content="{Binding TotalPersons}" 
                                                     ToolTip="Total de registros"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Persons}"
                              SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                              AutoGenerateColumns="False" CanUserAddRows="False"
                              IsReadOnly="True" SelectionMode="Single"
                              VerticalScrollBarVisibility="Auto"
                              materialDesign:DataGridAssist.CellPadding="12 8"
                              materialDesign:DataGridAssist.ColumnHeaderPadding="12 8">
                        <DataGrid.Columns>
                            <!-- Status Icon -->
                            <DataGridTemplateColumn Header="Status" Width="70">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <materialDesign:PackIcon Width="24" Height="24" HorizontalAlignment="Center">
                                            <materialDesign:PackIcon.Style>
                                                <Style TargetType="materialDesign:PackIcon">
                                                    <Setter Property="Kind" Value="CheckCircle"/>
                                                    <Setter Property="Foreground" Value="Green"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Kind" Value="CloseCircle"/>
                                                            <Setter Property="Foreground" Value="Red"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </materialDesign:PackIcon.Style>
                                        </materialDesign:PackIcon>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Nome -->
                            <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*" MinWidth="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Tipo -->
                            <DataGridTemplateColumn Header="Tipo" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="10" Padding="8,4" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#4CAF50"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Type}" Value="Supplier">
                                                            <Setter Property="Background" Value="#FF9800"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Type}" Value="Employee">
                                                            <Setter Property="Background" Value="#9C27B0"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Type}" Value="Both">
                                                            <Setter Property="Background" Value="#2196F3"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Type}" Foreground="White" FontSize="11" FontWeight="Medium"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Email -->
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220"/>

                            <!-- Telefone -->
                            <DataGridTextColumn Header="Telefone" Binding="{Binding PhoneNumber}" Width="120"/>

                            <!-- Documento -->
                            <DataGridTextColumn Header="NIF/CPF" Binding="{Binding TaxId}" Width="140"/>

                            <!-- Cidade -->
                            <DataGridTextColumn Header="Cidade" Binding="{Binding City}" Width="140"/>

                            <!-- Ações -->
                            <DataGridTemplateColumn Header="Ações" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                    Style="{StaticResource MaterialDesignIconButton}"
                                                    ToolTip="Editar"
                                                    Width="32" Height="32">
                                                <materialDesign:PackIcon Kind="Pencil" Width="18" Height="18"/>
                                            </Button>

                                            <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                    Style="{StaticResource MaterialDesignIconButton}"
                                                    ToolTip="Excluir"
                                                    Foreground="#F44336"
                                                    Width="32" Height="32">
                                                <materialDesign:PackIcon Kind="Delete" Width="18" Height="18"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Pagination -->
                    <Border Grid.Row="2" Background="{DynamicResource MaterialDesignToolBarBackground}" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Page Info -->
                            <TextBlock Grid.Column="0" VerticalAlignment="Center">
                                <Run Text="Página"/>
                                <Run Text="{Binding CurrentPage}" FontWeight="Bold"/>
                                <Run Text="de"/>
                                <Run Text="{Binding TotalPages}" FontWeight="Bold"/>
                                <Run Text="•"/>
                                <Run Text="{Binding TotalItems}"/>
                                <Run Text="registros"/>
                            </TextBlock>

                            <!-- Page Controls -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Command="{Binding FirstPageCommand}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Primeira página">
                                    <materialDesign:PackIcon Kind="PageFirst"/>
                                </Button>
                                <Button Command="{Binding PreviousPageCommand}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Página anterior">
                                    <materialDesign:PackIcon Kind="ChevronLeft"/>
                                </Button>
                                <Border Background="{DynamicResource PrimaryHueMidBrush}" 
                                        CornerRadius="4" Padding="12,6" Margin="8,0">
                                    <TextBlock Text="{Binding CurrentPage}" 
                                               Foreground="White" 
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <Button Command="{Binding NextPageCommand}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Próxima página">
                                    <materialDesign:PackIcon Kind="ChevronRight"/>
                                </Button>
                                <Button Command="{Binding LastPageCommand}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Última página">
                                    <materialDesign:PackIcon Kind="PageLast"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </materialDesign:Card>

        </Grid>
    </Grid>
</Page>

