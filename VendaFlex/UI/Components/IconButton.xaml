<UserControl x:Class="VendaFlex.UI.Views.Components.IconButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="200">
    
    <Button x:Name="RootButton"
            Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Padding="16,0"
            Cursor="Hand"
            Click="RootButton_Click">
        <Button.Style>
            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding ButtonStyle, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="Secondary">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
        
        <StackPanel Orientation="Horizontal">
            <materialDesign:PackIcon x:Name="IconElement"
                                   Width="20" Height="20"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
            <TextBlock x:Name="TextElement"
                     VerticalAlignment="Center"
                     Margin="8,0,0,0"/>
        </StackPanel>
    </Button>
</UserControl>
